- **步骤 1 — SAE 特征提取：** 用稀疏自编码器从锚点数据与当前可获得的数据中得到可解释的任务相关特征。
- **步骤 2 — 识别缺失特征：** 对比锚点集与当前可获得数据中所覆盖的特征，其差集为 **F<sub>miss</sub>**（任务需要但合成尚未覆盖的特征）。
- **步骤 3 — 覆盖引导合成：** 对 F<sub>miss</sub> 中每个特征引导生成并按 SAE 激活筛选，使最终集合以更少但多样的样本覆盖任务相关特征。
